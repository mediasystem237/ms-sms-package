# My SMS Package

This PHP package allows you to send SMS using the NEXAH BulkSMS API.

## Installation

You can install the package via Composer:

```bash
composer require votre_nom_utilisateur/my-sms-package
